DROP DATABASE IF EXISTS pic_stat;

CREATE DATABASE IF NOT EXISTS pic_stat;

USE pic_stat;

CREATE TABLE rate_it(
    id INT AUTO_INCREMENT PRIMARY KEY,
    hearts DECIMAL(4,2),
    up DECIMAL(4,2),
    down DECIMAL(4,2)
); 